{
  "name": "downloads-toolchain",
  "private": true,
  "description": "Smart contract + ARTB trading toolchain helpers",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "verify": "bash scripts/verify-toolchain.sh",
    "bootstrap": "bash scripts/wsl-bootstrap.sh"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "hardhat": "^3.12.0",
    "solc": "^0.8.36"
  }
}
